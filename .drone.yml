workspace:
  base: /app
  path: schwant
services:
  web:
    image: fpfis/php56-build
pipeline:
  composer-install:
    images: fpfis/php56-build
    commands:
      - composer install
  check_http:
    images: fpfis/php56-build
    commands:
      - wget http://127.0.0.1
